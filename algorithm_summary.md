# Lotto Genius Pro: 로또 번호 생성 알고리즘 핵심 요약

Lotto Genius Pro 웹앱에 구현된 로또 번호 생성 알고리즘은 **가중치 기반의 룰렛 추첨 방식**과 **7가지 통계적/패턴 방어 필터**로 구성되어 있습니다.

## 1단계: 지능형 가중치 룰렛 추첨 (Roulette Wheel Selection)
모든 번호(1~45)의 기본 가중치를 10으로 먼저 설정한 뒤, 과거 출현 데이터를 바탕으로 가중치를 재조정하여 번호 6개를 뽑습니다.
* **콜드 번호 (Cold Numbers):** 최근 15주(15회차) 이상 한 번도 출현하지 않은 번호는 가중치를 **30 (확률 3배 증가)**으로 대폭 높입니다.
* **핫 번호 (Hot Numbers):** 과거 당첨 빈도수가 가장 높은 10개의 인기 번호는 가중치를 **5 (확률 반감)**로 낮추어 과열을 막습니다.

## 2단계: 7중 디펜시브 필터 검증 (Pro Logic Filters)
1단계에서 뽑힌 6개의 후보 번호 조합은 아래의 7가지 엄격한 통계 및 패턴 필터링을 거치게 되며, 하나라도 어긋나면 가차 없이 탈락되고 새로운 번호를 다시 뽑습니다.

1. **합계 밸런스 필터:** 역대 모든 당첨 번호의 '평균 합계'를 구한 뒤, 사용자가 설정한 허용 오차 범위(기본 ±5% 또는 엄격 ±2%) 내에 후보의 번호 합이 들어오는지 확인합니다.
2. **연속 번호 배제:** `1, 2, 3, 4`와 같이 4개 이상의 번호가 연속으로 이어지는 기형적인 조합을 배제합니다.
3. **과열 번호 중복 배제:** 핫 번호(최다 빈출 번호 상위 10개)가 한 조합에 4개 이상 쏠려있는 경우를 배제합니다.
4. **생일 패턴 배제:** 사람들이 수동으로 주로 찍는 날짜 번호(모든 번호가 31 이하인 경우)를 통계적 이점을 위해 추첨에서 배제합니다.
5. **홀짝 극단 쏠림 배제:** 홀수와 짝수의 비율이 `0:6`, `6:0`, `1:5`, `5:1` 처럼 극단적으로 치우친 조합을 제거합니다. (가장 잘 나오는 3:3, 2:4, 4:2 패턴만 통과)
6. **끝수 집중도 배제 (New):** `3, 13, 23, 33`과 같이 일의 자리 숫자가 동일한 번호가 4개 이상 포함된 경우를 배제합니다.
7. **직전 회차/역대 1등 조합 회피 (New):**
   * 최신 직전 당첨 번호와 4개 이상 겹치는 조합은 제외시킵니다.
   * 로또 역사상 모든 과거 1등 당첨 내역과 비교하여, 기존 1등 번호와 **5개 이상 완벽히 일치하는 조합**은 다시 당첨될 확률이 극히 낮으므로 필터링합니다.

## 3단계: 루프 및 결과 반환
이러한 까다로운 조건을 모두 만족하는 추천 가능한 조합이 나올 때까지 위 과정을 반복(최대 50,000회 연산 제한)하여 사용자가 설정한 목표 게임 수(최대 50게임)를 채우고 화면에 결과를 반환합니다.

> **💡 요약:** 이 알고리즘은 오랫동안 안 나온 번호를 밀어주고, 너무 뻔하거나 이미 나왔던 불리한 패턴을 수학적으로 철저하게 깎아내는 방식입니다.

===

Lotto Genius Pro 웹앱에 구현된 로또 번호 생성 알고리즘은 가중치 기반의 룰렛 추첨 방식과 7가지 통계적/패턴 방어 필터로 구성되어 있습니다.

코드를 분석하여 정리한 핵심 생성 로직은 다음과 같습니다.

1단계: 지능형 가중치 룰렛 추첨 (Roulette Wheel Selection)
모든 번호(1~45)의 기본 가중치를 10으로 먼저 설정한 뒤, 과거 출현 데이터를 바탕으로 가중치를 재조정하여 번호 6개를 뽑습니다.

* 콜드 번호 (Cold Numbers): 최근 15주(15회차) 이상 한 번도 출현하지 않은 번호는 가중치를 **30 (확률 3배)**으로 대폭 높입니다.
* 핫 번호 (Hot Numbers): 과거 당첨 빈도수가 가장 높은 10개의 인기 번호는 가중치를 **5 (확률 반감)**로 낮추어 과열을 막습니다.

2단계: 7중 디펜시브 필터 검증 (Pro Logic Filters)
1단계에서 뽑힌 6개의 후보 번호 조합은 아래의 7가지 엄격한 통계 및 패턴 필터링을 거치게 되며, 하나라도 어긋나면 가차 없이 탈락되고 새로운 번호를 다시 뽑습니다.

1. 합계 밸런스 필터: 역대 모든 당첨 번호의 '평균 합계'를 구한 뒤, 사용자가 설정한 허용 오차 범위(기본 ±5% 또는 엄격 ±2%) 내에 후보의 번호 합이 들어오는지 확인합니다.
2. 연속 번호 배제: 1, 2, 3, 4와 같이 4개 이상의 번호가 연속으로 이어지는 기형적인 조합을 배제합니다.
3. 과열 번호 중복 배제: 핫 번호(최다 빈출 번호 상위 10개)가 한 조합에 4개 이상 쏠려있는 경우를 배제합니다.
4. 생일 패턴 배제: 사람들이 수동으로 주로 찍는 날짜 번호(모든 번호가 31 이하인 경우)를 통계적 이점을 위해 배제합니다.
5. 홀짝 극단 쏠림 배제: 홀수와 짝수의 비율이 0:6, 6:0, 1:5, 5:1 처럼 극단적으로 치우친 조합을 제거합니다. (가장 잘 나오는 3:3, 2:4, 4:2 조합만 통과)
6. 끝수 집중도 배제: 3, 13, 23, 33과 같이 일의 자리 문자(끝수)가 동일한 번호가 4개 이상 포함된 경우를 배제합니다.
7. 직전 회차/역대 1등 조합 회피:
   * 최신 직전 당첨 번호와 4개 이상 겹치는 조합은 제외시킵니다.
   * 로또 역사상 모든 과거 1등 당첨 번호 내역과 비교하여, 기존 1등 번호와 5개 이상 완벽히     
     일치하는 조합은 다시 당첨될 확률이 극히 낮으므로 모두 회피시킵니다.

3단계: 루프 및 결과 반환
이러한 까다로운 조건을 모두 만족하는 진짜 추천 가능한 조합이 나올 때까지 위 과정을 반복(최대 50,000회 연산 제한)하여 사용자가 설정한 목표 게임 수(최대 50게임)를 채우고 결과를 화면에 보여줍니다.

즉, 이 알고리즘은 오랫동안 안 나온 번호를 밀어주고, 너무 뻔하거나 이미 나왔던 불리한 패턴을 수학적으로 철저하게 깎아내는 방식입니다.